[project]
name = "extractor"
version = "0.1.0"
description = "Extractor de datos de documentos contables (prototipo)"
requires-python = ">=3.11"
dependencies = [
    "pymupdf>=1.24",
    "pyyaml>=6.0",
    "rapidfuzz>=3.0",
]

[tool.setuptools.packages.find]
where = ["src"]
